# Plan

1. Add `src/surval/droid_policy_reference.py`: build frozen policy StateDatabase and metadata from validated cache; use existing row/GT alignment, geometry, fixed-half errors and score reduction. Expose same-reference OMN via existing projection implementation. Validate source epoch, features, rows and same training run.
2. Add `scripts/tune_droid_policy_reference.py` plus thin `.sh`/`.md`: three CPU task workers, default epoch25, 2880 HPs, reuse existing `execute`/atomic output/status/hash helpers. Save DB, quantile tables, score/OMN checkpoint shards with resume signatures. Query all targets in reference space only. Preserve old DINO artifacts.
3. Report via same CLI: verify inputs/labels, select balanced common/task-specific HP for full and requested Ta, include frozen-DINO-HP controls, aggregate matched H8 MSE and DINO/Policy25 OMN plus clearly labeled old partial baselines. Re-score every reported Policy25 HP with canonical thresholds/scorer; audit DINO controls under current code before trusting historical scores.
4. Add focused unittest module: zero/bad epoch/cross-run/row/GT rejection; target features ignored; odd/even top-half fixed-grid canonical agreement; order invariance; matched OMN invariance and kNN exclusion; CLI no-write matrix and shards. Run existing runner success/failure probes. No production DINO code refactor or old file edits needed.
5. `bash -n`, ShellCheck if available (not installed currently; record unavailable), no-write list/dry-run/shards, unittest. Launch private CPU supervisor in new `outputs/droid_policy25_val30_20260915` after tests; no GPU/no service exposure. Observe jobs and report process through completion; stop private supervisor afterward.
6. Verify 8640 task/HP metrics, 86400 checkpoint scores,30 baseline pairs, canonical selection identity, output/source checksums. Save aggregate report/HP/checkpoint tables and loop evidence. Repair at most3 scoped attempts.

Score compatibility: current DROID loader supports additional producers but archived DROID scores remain immutable. Record current code signatures and canonically verify published DINO controls instead of assuming old/current source hashes are identical. OMN representation comparison uses the same H15-cache H8 prefix, not old stochastic H8 draws. No native16 padding.
