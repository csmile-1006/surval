# Shared DINO DB / full-validation OMN: complete

R1–R5 satisfied. User explicitly selected full-val-split DINO neighbors independent
of checkpoint and batch. Implemented with existing encoder, state/history,
FAISS database, local-threshold and projection code; no new runtime dependency.

New entrypoint: `scripts/build_droid_dino_db.py`. Scorer option:
`--dino-db-root`; OMN neighbor count: `--dino-omn-k` (default5). Optional metric:
`Off_Manifold_Norm_DINO`, automatically included by custom-outcome aggregation.
Existing policy feature path and original cached baseline values are preserved.

Verification:60 related CPU tests passed;5 DINO contract tests rerun with extra
horizon/sample-isolation assertions. GPU smoke used actual apple val5 images,
488 rows,2 views x2 history x768 CLS dimensions. All10 cached checkpoints share
one DB/threshold artifact and have5 eligible neighbors per row. Independent
cosine retrieval and seven-way batch partition agree. Original120 HDF5 caches
retain the exact pre-build SHA256 manifest.

One repair: Octo resize argument corrected from scalar224 to[224,224]; actual
data-root signatures and loaded DB/config agreement checked. Broader pytest
initially lacked pytest/tomli in the overlay; reused installed pure-Python pytest
with explicit test/config paths, without changing package installations. Exact
commands and failures are retained in test-results-1.json/test-results-2.json.
Native patch updates failed because bwrap namespaces are unavailable; source
diffs were created via apply_patch and applied via approved `git apply` after
`git apply --check`. No unrelated dirty-tree changes were reverted.

Artifacts:

- DB: `outputs/shared_dino_val/pick_up_the_apple_and_place_it_on_the_dish/droid/val5`
- Scores: `outputs/droid_dino_apple_val5_20260912/checkpoint_metrics.csv` (+JSON)
- Recheck: `PYTHONPATH=src OMP_NUM_THREADS=4 OPENBLAS_NUM_THREADS=4 .venv-droid/bin/python .loop-engineering/20260912T173303Z-shared-dino/verify_real.py`
- Full build/score instructions: `docs/DROID.md`, shared-DINO section.

Other11 split DBs were intentionally not generated by this bounded verification.
No real-world outcomes were invented. Changing val size still changes the
reference manifold; common DINO fixes representation/batch/checkpoint dependence,
not this chosen validation-size dependence. No commits, pushes or services changed.

Loop artifacts: `.loop-engineering/20260912T173303Z-shared-dino/`.
