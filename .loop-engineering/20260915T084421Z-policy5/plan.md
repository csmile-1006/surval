# Plan

1. Hash every epoch25/50 output before edits; preserve unrelated dirty worktree.
2. Reuse unchanged worker and launcher with explicit `--reference-epoch 5` and
   `--output-root outputs/droid_policy5_val30_20260915`; same3CPU jobs/2880grid.
3. Extend existing report from supported25/50 to5/25/50. Share the existing
   non25 comparison path and dynamic labels; preserve25/50 output roots.
   Reproduce archived25 metrics and equal input/cache/kernel/fixed-setting
   signatures (reference epoch excepted),non-reference baselines exactly equal.
4. Extend existing tests to explicit5 reference/target-feature invariance and
   no-write shards,retain50 reference support,guard old output roots. Update
   existing runbook with5 commands; no production launcher/report copy.
5. Run CPU tests/static/dryrun,then private supervisor workers and report.
   Original scorer/threshold audit every new5 choice and DINO control,all10
   checkpoints; prior25 optimum already audited,verify hashes/scalar values.
   Matched H8 baseline MSE/DINO/Policy5 OMN; preserve25 OMN comparison.
6. Verify8640 scalar metrics,full selected-point canonical coverage,macro means,
   previous25/50 bytes,completed-worker resume; save report/HP/score CSVs,stop
   private supervisor,and close loop. Optional absent pytest/ShellCheck remain
   explicitly unverified. Max3 scoped repairs; no extra dependency or upload.
